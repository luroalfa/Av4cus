import React from "react";
import { StyledSubtitle } from "./Style/SubtitleStyleElements";

const Subtitle = ({ children }) => {
  return <StyledSubtitle>{children}</StyledSubtitle>;
};

export default Subtitle;
